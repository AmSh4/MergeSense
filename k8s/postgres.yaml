
apiVersion: v1
kind: Service
metadata:
  name: MergeSense-postgres
spec:
  ports:
    - port: 5432
  selector:
    app: MergeSense-postgres
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: MergeSense-postgres
spec:
  selector:
    matchLabels:
      app: MergeSense-postgres
  template:
    metadata:
      labels:
        app: MergeSense-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          env:
            - name: POSTGRES_DB
              value: "codeflow"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
          ports:
            - containerPort: 5432
